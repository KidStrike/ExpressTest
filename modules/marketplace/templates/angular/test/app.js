// Declare app level module which depends on filters, and services
/*angular.module('myApp', ['myApp.filters', 'myApp.services', 'myApp.directives']).
  config(['$routeProvider', '$locationProvider', function($routeProvider, $locationProvider) {
    $routeProvider.
      when('/marketplace/partners/', {
        templateUrl: '/partials/index',
        controller: IndexCtrl
      }).
      when('/marketplace/partners/addPost', {
        templateUrl: '/marketplace/partners/partials/addPost',
        controller: AddPostCtrl
      }).
      when('/marketplace/partners/readPost/:id', {
        templateUrl: '/marketplace/partners/partials/readPost',
        controller: ReadPostCtrl
      }).
      when('/marketplace/partners/editPost/:id', {
        templateUrl: '/marketplace/partners/partials/editPost',
        controller: EditPostCtrl
      }).
      when('/marketplace/partners/deletePost/:id', {
        templateUrl: '/marketplace/partners/partials/deletePost',
        controller: DeletePostCtrl
      }).
      otherwise({
        redirectTo: '/marketplace/partners/'
      });
    $locationProvider.html5Mode(true);
  }]);*/